{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "ios-main",
  "windows": ["main"],
  "platforms": ["iOS"],
  "permissions": [
    "core:default",
    "dialog:allow-save",
    "fs:allow-write-file",
    "fs:allow-remove",
    {
      "identifier": "http:default",
      "allow": [{ "url": "http://*/*" }, { "url": "https://*/*" }]
    }
  ]
}
